---
const { title, keywords, description, pagecover, activeNav, Home } = Astro.props;
// 网站配置
import SITE_INFO from "@/config";
const { GoogleAds, Theme, HomeBanner } = SITE_INFO;
const { ad_Client, asideAD_Slot, articleAD_Slot } = GoogleAds;
// Head 依赖
import Head from "@/components/Head/Head.astro";
// 顶部 Header
import Header from "@/components/Header/Header.astro";
// Main 区域 Header
import MainHeader from "@/components/MainHeader/MainHeader.astro";
// Aside组件
import Aside from "@/components/Aside/Aside.astro";
// 底部 Footer
import Footer from "@/components/Footer/Footer.astro";
// 返回顶部
import BackTop from "@/components/BackTop/BackTop.astro";
// 手机端侧边栏
import MobileSidebar from "@/components/MobileSidebar/MobileSidebar.astro";
// A Modern CSS Reset
import "@/styles/Reset.less";
// 全局基础样式
import "@/styles/Base.less";
// Layout 样式
import "./Layout.less";
// Hero 模式：只有「首页」且 Banner 本体开着时才成立。
// HomeBanner.enable=false 时 MainHeader 不渲染（连「向下滚动」提示一起没了），
// 再把 Header/内容/Footer 藏起来就只剩一屏纯白，没有任何可视线索
const heroMode = !!Home && HomeBanner.enable;
// Hero 模式下 body 需要的 class 与「JS 挂了就自动降级」的兜底脚本。
// 刻意不在服务端直出 vh-home：那样一旦 Init 的 chunk 加载失败或 JS 被禁用，
// 内容会永远停在 opacity:0 + pointer-events:none，整页只剩 Hero
const heroBootScript = `document.body.classList.add("vh-home");window.__vhHeroFallback=setTimeout(function(){document.body.classList.remove("vh-home","vh-home-revealed","vh-header-peek")},8000)`;
---

<html lang="zh-CN">
	<Head Title={title} Keywords={keywords} Description={description} PageCover={pagecover}>
		<!-- 谷歌广告JS加载项 -->
		{ad_Client && (asideAD_Slot || articleAD_Slot) && <script is:inline async src={`https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=${ad_Client}`} crossorigin="anonymous" />}
		<!-- 彩旗🎈 -->
		<script is:inline src="/assets/js/vhCaiqi.js" defer></script>
		<!-- 设置主题颜色 -->
		<Fragment
			set:html={`<style>:root {${Object.entries(Theme)
				.map(([key, value]) => `${key}:${value};`)
				.join("")}--vh-main-header-height:${heroMode ? HomeBanner.HomeHeight : HomeBanner.PageHeight};--vh-home-banner:${HomeBanner.background}}</style>`}
		/>
	</Head>
	<body>
		{heroMode && <script is:inline set:html={heroBootScript} />}
		<MobileSidebar />
		<Header activeNav={activeNav} />
		<main class="main">
			{HomeBanner.enable && <MainHeader />}
			<section class="main-inner" style={`padding-top:${HomeBanner.enable ? "0.88rem" : "calc(66px + 0.68rem)"}`}>
				<section class="main-inner-content">
					<slot />
				</section>
				<Aside />
			</section>
			<BackTop />
		</main>
		<Footer />
		<script>
			import InitFn from "@/scripts/Init";
			// 全局初始化
			InitFn();
		</script>
	</body>
</html>
